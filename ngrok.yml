# Это стандартная версия для современных конфигурационных файлов ngrok
version: "2"

# ВАЖНО: Вставьте сюда ваш личный токен аутентификации от ngrok.
# Вы можете получить его бесплатно после регистрации на их сайте: https://dashboard.ngrok.com/get-started/your-authtoken
# Это дает вам больше возможностей, например, более стабильные сессии.
authtoken: 2yGxDTRIjRKfAmoLA9HHQl8981H_21MwDV2N1aiWnUnbHqb7Y

# Секция, где мы описываем все туннели, которые хотим запустить
tunnels:

  # Название первого туннеля. Мы назовем его "backend" для понятности.
  backend:
    # Протокол, который мы пробрасываем (в нашем случае, http)
    proto: http
    # Локальный адрес и порт, на котором работает наш бэкенд (FastAPI)
    addr: 8000

  # Название второго туннеля. Назовем его "frontend".
  frontend:
    # Протокол тот же
    proto: http
    # Локальный адрес и порт, на котором работает наш фронтенд (Flutter)
    addr: 8080